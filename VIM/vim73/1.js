if (condition) {
    //code
}
function function_name (argument) {
    // body...
}
function function_name (argument) {
    // body...
    console.log('HelloWorld');
}
function howToDo (argument) {
    // body...
    var a = 1;
        b = 2;
    return a + 1;
}
$('#abc').change(function (e) {
    $('somequery').click(function(event) {
       somequery.click(function(event) {
           e.preventDefault();
       });
    })
    somequery.change(function (e) {
    })
})  
$('somequery').click(function(event) {
})
$('somequery').click(function(event) {
    var $self = $(this);
    $self.find('ele').each(function(i,d) {
        // Act on the event
        if (isFlag) {
        }
        var flag = $self.hasClass('main');
        if (flag && 1 == 2) {
            flag = !falg;
            return flag;
        }
        return flag;
    });
})
    这是新的一段落

